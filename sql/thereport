/*
Enter your query here.
Please append a semicolon ";" at the end of the query and enter your query in a single line to avoid error.
*/
SELECT CASE WHEN grade<8 then 'NULL' else s.Name end, g.GRADE, s.marks FROM Students s, grades g where s.marks between g.min_mark and g.max_mark order by grade desc, s.name;


