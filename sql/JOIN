SELECT c.NAME 
FROM CITY c
JOIN COUNTRY co
ON c.COUNTRYCODE = co.CODE
WHERE CONTINENT = 'Africa';

SELECT SUM(c.POPULATION) 
FROM CITY c
JOIN COUNTRY co
ON c.COUNTRYCODE = co.CODE
WHERE CONTINENT = 'Asia';
